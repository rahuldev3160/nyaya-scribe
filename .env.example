# Copy this file to .env for local development (never commit .env)
# On Streamlit Cloud, set these as Secrets in the app settings.

# Required: Anthropic API key for quiz evaluation
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: Pin a fixed user ID (leave blank for session-scoped UUIDs in public mode)
IES_USER_ID=
